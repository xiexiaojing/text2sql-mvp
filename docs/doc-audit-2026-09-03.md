# Doc Audit 2026-09-03

对账目标：`docs/architecture.md` 与 `docs/mcp-integration.md` 里出现的每一个模块名 / 函数名 /
配置键 / status 取值 / MCP 工具签名，逐条与代码对齐。

对账基准 commit：`3229ee1`

## architecture.md

| 文档说 | 代码是 | 结论 |
|--------|--------|------|
| Request flow 三分支：可执行 → SQL模板编译；受控兜底 → Schema/LLM 兜底；需映射 → 拒答 | `service.py` L242-272 依 `semantic_plan.status` 分派：`guarded_text2sql` 走 schema/LLM，`executable` 走 `business_semantics.compile()`；其它状态抛 `RejectedQuery` | 一致 |
| status = `executable` / `guarded_text2sql` / `needs_mapping` 三个配置值 | `service.py` L30 `_EXECUTABLE_STATUSES = {"executable", "guarded_text2sql", "metadata"}`；`business_semantics.py` L165 默认 `needs_mapping`；额外还会产出 `unsupported`、`needs_clarification`、`metadata` | 文档只列了 3 个配置层级 status，代码运行时另有 `metadata`（`field_explanation` 元数据查询）、`unsupported`（路由拒绝）、`needs_clarification`（缺槽位）。原表本身没错，但漏了 `metadata` 作为可配置意图状态 —— 已在文档补一列 |
| 术语对照 `护栏 = Harness` | 代码内 SQL 护栏类叫 `SqlGuard`（`sql_guard.py`） | 术语原文用 Harness 只是英文类比；类名是 `Guard`。术语表保留，同义 |
| 规则引擎实现：`FollowUpRule(id, priority, rewrite)` | `conversation_rewrite.py` L46-49 `FollowUpRule` dataclass 字段完全一致 | 一致 |
| `_build_rules() → _RULES` | `conversation_rewrite.py` L355-364 | 一致 |
| 入口 `conversation.py → contextualize_question()` | `conversation.py` L9 | 一致 |
| 调度器 `conversation_rewrite.py → apply_follow_up_rewrites()` | `conversation_rewrite.py` L52 | 一致 |
| 共享工具 `conversation_context.py` | 存在，含 `normalize_history` / `looks_like_follow_up` / `previous_user_question` | 一致 |
| 按 `priority` 降序、第一个成功即停 | `apply_follow_up_rewrites()` L61 `for rule in _RULES: ... if result: return`；`_build_rules()` 用 `sorted(..., key=-rule.priority)` | 一致 |
| 对话改写规则表 priority 100 `chart_type_follow_up` | `conversation_rewrite.py` L357 一致 | 一致 |
| priority 91 `count_to_list_follow_up` | L358 一致 | 一致 |
| priority 10 `dimension_slot_follow_up` | L359 一致 | 一致 |
| priority `95-89` 领域专用规则 `person_count / grid / ledger …` | 代码里无此三条规则；`__pycache__/` 仍留有 `grid_follow_up.pyc`/`ledger_follow_up.pyc` 是 4d1213b 重构前遗留的字节码，`.py` 已删除。当前 `_RULES` 就是 3 条 | **文档漂移**：删除该行 |
| 改写日志 `kind=conversation`, `rewriteReason`, `originalQuestion`, `effectiveQuestion` | `conversation_rewrite.py` L64-71 交互日志字段完全一致 | 一致 |
| Whitelist schema `configs/whitelist_tables.yaml` | 存在；`domain_column: tenant_id` | 一致 |
| 每个 scoped table 声明 `domain_column`（demo: `tenant_id`），runtime 自动注入 `table.domain_column = %(domain_id)s` | `sql_policy.py` L14-27 `inject_domain_filter()` 完全就是这个逻辑 | 一致 |
| Business semantics `configs/business_semantics.yaml` | 存在 | 一致 |
| intent 含 lexical `match` / `semantic.queries` / `status` / `template` | `business_semantics.py` L167-192 解析这些字段 | 一致 |
| SQL policy: SELECT-only / 参数化敏感值 / 自动 LIMIT / EXPLAIN 拒绝 | `sql_guard.py` L100-109（SELECT 校验）、`sql_policy.py` L30-40（ensure_limit）、`router.py` L98-101（`reject_if_explain_too_large`） | 一致 |
| Execution modes `dry_run`（默认） / `live` | `config.py` L127-132 `live_execution` 属性；默认 `TEXT2SQL_EXECUTION_MODE=dry_run`（L197） | 一致 |
| HTTP 表面 `text2sql_api`：`/v1/query`, `/v1/query/estimate`, audit APIs, `/chat` | `apps/api/src/text2sql_api/main.py`：`/v1/query`（L390）、`/v1/query/estimate`（L416）、`/v1/audit/unsupported`（L427）、`/v1/audit/{query_id}`（L436）、`/chat`（L47）；额外还有 `/health`、`/v1/memories`、`/v1/schema/summary`、`/v1/evals/run` | 一致（文档写的是概述，未逐条列全，不视作漂移） |
| MCP 表面 `text2sql_mcp` 用同一个 `Text2SqlService` | `apps/mcp/src/text2sql_mcp/server.py` L14-16 `service()` 返回 `Text2SqlService.from_project_root()` | 一致 |
| 自定义步骤引用 `scripts/introspect_schema.py` | 该脚本存在 | 一致 |
| Demo domain: `payment_order` 有 channel/amount/status/payer_mobile | `configs/whitelist_tables.yaml` L15-20 完全一致，`payer_mobile` 标 `sensitive: true` | 一致 |

## mcp-integration.md

| 文档说 | 代码是 | 结论 |
|--------|--------|------|
| 工具 `health` | `server.py` L20 `@mcp.tool() def health()` | 一致 |
| 工具 `query` | `server.py` L34 签名 `(question, domain_id, user_id, allow_return_sql, force_llm, max_rows, history)` | 一致 |
| 工具 `estimate` | `server.py` L65 签名 `(question, domain_id, history)` | 一致 |
| 工具 `schema_summary` | `server.py` L81 | 一致 |
| 工具 `audit` | `server.py` L93 签名 `(query_id)` | 一致 |
| 工具 `unsupported_questions` | `server.py` L102 签名 `(limit=50, since_ms=None)` | 一致 |
| Resource `text2sql://schema/summary` | `server.py` L86 `@mcp.resource("text2sql://schema/summary")` | 一致 |
| 本地运行入口 `python -m text2sql_mcp.server` | `server.py` L110-111 `def main(): mcp.run()`；且 L161 `if __name__ == "__main__": main()` | 一致 |
| 安装后可用 `text2sql-mcp` | `pyproject.toml` L20 `text2sql-mcp = "text2sql_mcp.server:main"` | 一致 |
| 环境变量 `TEXT2SQL_PROJECT_ROOT` | `config.py` L136 | 一致 |
| 环境变量 `TEXT2SQL_EXECUTION_MODE` | `config.py` L197 | 一致 |
| 环境变量 `TEXT2SQL_EXECUTOR_BACKEND=mysql_mcp` | `config.py` L198；`executor.py` L206 接受 `{"mcp", "mcp_mysql", "mysql_mcp"}` | 一致 |
| 环境变量 `TEXT2SQL_MYSQL_MCP_COMMAND` | `config.py` L177 | 一致 |
| 环境变量 `TEXT2SQL_MYSQL_MCP_ARGS` | `config.py` L178 用 `shlex.split()` | 一致 |
| 兼容层 `text2sql_mcp.mysql_mcp_compat` | `apps/mcp/src/text2sql_mcp/mysql_mcp_compat.py` 存在 | 一致 |
| 兼容层可按需 `ssl_disabled=True` | `mysql_mcp_compat.py` L12-14 读 `MYSQL_MCP_SSL_DISABLED` 环境变量 | 一致 |

## 配置键对齐

| 文档 / 配置文件 | 文档提到的键 | 代码/配置里的键 | 结论 |
|-----------------|--------------|-----------------|------|
| `configs/whitelist_tables.yaml` | tables, columns, row counts, indexes, join paths, `domain_column`, `sensitive` | 全部存在 | 一致 |
| `configs/business_semantics.yaml` | intents 的 `match` / `semantic.queries` / `status` / `template`；`sql_templates` | 全部存在；配置还额外有 `entities`、`entity_query_schemas`（`dynamic_entity_query`）—— 文档未提，但那是补充能力，不算漂移 | 一致 |
| `.env.local` | `TEXT2SQL_EXECUTOR_BACKEND` / `TEXT2SQL_MYSQL_MCP_COMMAND` / `TEXT2SQL_MYSQL_MCP_ARGS` | 全部存在 | 一致 |

## 需要修的漂移

1. `docs/architecture.md` 第 118 行 `95–89 person_count / grid / ledger …` —— 代码已无此规则，删。
2. 两份文档补一个「本文对应 commit」注脚，方便下次对账时对齐锚点。
3. `docs/architecture.md` status 表格加上 `metadata` 一行（`field_explanation` 意图当前就是 `metadata` 状态）—— 否则新人照抄不到字段解释路径。
4. 加防漂移测试：从 architecture.md status 表格解析取值，与代码 `_EXECUTABLE_STATUSES ∪ 其它已知 status` 对齐。
